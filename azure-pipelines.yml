trigger: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  RG: 'RG_Manu_Coppens'  # Correctie van naamgeving
  subscriptionId: 'cc934d76-6d72-49cb-a908-81217ad4ae29'  # Vervang door je echte Subscription ID
  location: 'West Europe'

jobs:
  - job: Deploy
    steps:
      - task: AzureResourceManagerTemplateDeployment@3
        displayName: 'Deploy Bicep files'
        inputs:
          deploymentScope: 'Resource Group'
          azureResourceManagerConnection: 'VBt8Q~mZDDbSXGxTcOe3t6_d2-UDX8vCCkD3TbRY'  # Vervang door jouw service connection
          subscriptionId: '$(subscriptionId)'
          action: 'Create Or Update Resource Group'
          resourceGroupName: '$(RG)'
          location: '$(location)'
          templateLocation: 'Linked artifact'
          csmFile: 'main.bicep'
          deploymentMode: 'Validation'
